apiVersion: v1
kind: Service
metadata:
  namespace: c66-cluster-traefik
  name: geo
  labels:
    app.kubernetes.io/name: geo
spec:
  ports:
  - port: 9912
    targetPort: 9912
  selector:
    app.kubernetes.io/name: geo
